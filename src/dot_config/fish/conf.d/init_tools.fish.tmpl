{{output "fish" "-c" `
if command -v starship >/dev/null
    command starship init fish --print-full-init
end
` | trim}}

{{output "fish" "-c" `
if command -v zoxide >/dev/null
    zoxide init fish
end
` | trim}}
