# Generated from a Git alias by prepare_alias
# Defined via `source`
function cmgrst --wraps='chezmoi git -- reset' --description 'alias cmgrst=chezmoi git -- reset'
    chezmoi git -- reset $argv
end
