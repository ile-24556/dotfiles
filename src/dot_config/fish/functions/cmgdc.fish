# Generated from a Git alias by prepare_alias
# Defined via `source`
function cmgdc --wraps='chezmoi git -- diff --cached' --description 'alias cmgdc=chezmoi git -- diff --cached'
    chezmoi git -- diff --cached $argv
end
